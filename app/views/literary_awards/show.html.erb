<% @awards.each do |award| %>
    <%= link_to award.name, check_path(id: award.id) %>
<%end%>

<% if @my_book && @my_book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@my_book.errors.count, "error") %> prohibited this book from being saved:</h2>
    <ul>
      <% @my_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>